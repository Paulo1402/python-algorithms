[project]
name = "python-algorithms"
version = "0.1.0"
description = "Python algorithms"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.5",
    "pytest-watch>=4.2.0",
]
